version: '3.8'

services:
  cartelas:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cartelas
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - cartelas_fonts:/tmp/cartelas_fonts
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o-mini}
      - BASE_URL=${BASE_URL}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  cartelas_fonts:
